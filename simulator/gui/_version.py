"""
_version.py

Single source of truth for APP_VERSION.
Imported by main_window.py, settings_page.py, and any other module
that needs the version string — keeping them all decoupled.
"""
APP_VERSION: str = "2.0.5"

"""
simulator/gui/widgets
=====================
Reusable chrome widgets (not pages, not components).

Available modules:
    top_header    — TopHeader, FlowLayout
    sidebar       — PlugCard, Sidebar
    footer_bar    — FooterBar
    goodbye_screen — GoodbyeScreen
"""
# No eager imports — import directly from each sub-module.

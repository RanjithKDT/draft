"""
simulator/gui
=============
Hyva Simulator GUI package.

Structure:
    theme.py      — all visual tokens (colours, sizes, fonts)
    icons.py      — FA constants, Unicode symbols, IconWidget
    styles.py     — global QSS stylesheet (build_stylesheet)
    components.py — reusable UI building blocks
    fonts.py      — bundled font loading
    main_window.py — all application pages and main window
"""
